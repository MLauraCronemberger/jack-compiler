class Main {
    function void main() {
        var int x;
        let x = 10 + 20;
        let x = x * (-2);
        let x = arr[x + 1];
        do Output.printInt(x + 1);
        if (x > 0) {
            return;
        }
        return;
    }
}